language_servers:
  # ============================================================
  # DEFAULT PROFILE — Core LSP servers for everyday languages
  # ============================================================

  # JavaScript/TypeScript
  - name: typescript-language-server
    lspconfig_name: ts_ls
    languages: [typescript, javascript, javascriptreact, typescriptreact]
    installer: [npm, mason]
    provides_formatting: false

  # Go
  - name: gopls
    lspconfig_name: gopls
    languages: [go]
    installer: [go, mason]
    provides_formatting: true
    install_command: "golang.org/x/tools/gopls@latest"

  - name: golangci-lint-langserver
    lspconfig_name: golangci_lint_ls
    languages: [go]
    installer: [go, mason]
    provides_formatting: false
    install_command: "github.com/nametake/golangci-lint-langserver@latest"

  # Rust
  - name: rust-analyzer
    lspconfig_name: rust_analyzer
    languages: [rust]
    installer: [mise, mason]
    provides_formatting: true

  # Python
  - name: pyright
    lspconfig_name: pyright
    languages: [python]
    installer: [brew, mason]
    provides_formatting: false

  # Shell
  - name: bash-language-server
    lspconfig_name: bashls
    languages: [bash, sh, zsh]
    installer: [npm, mason]
    provides_formatting: false

  # Lua (needed for neovim config)
  - name: lua-language-server
    lspconfig_name: lua_ls
    languages: [lua]
    installer: [mise, mason]
    provides_formatting: true

  # Markup and data
  - name: json-lsp
    lspconfig_name: jsonls
    languages: [json, jsonc]
    installer: [mason]
    provides_formatting: false

  - name: yamlls
    mason_name: yamlls
    lspconfig_name: yamlls
    languages: [yaml]
    installer: [mason]
    provides_formatting: false

  - name: marksman
    lspconfig_name: marksman
    languages: [markdown]
    installer: [mise, mason]
    provides_formatting: false

  - name: markdown-oxide
    lspconfig_name: markdown_oxide
    languages: [markdown]
    installer: [cargo, mason]
    provides_formatting: false
    install_command: "cargo install --locked --git https://github.com/Feel-ix-343/markdown-oxide.git markdown-oxide"

  # Spelling/typos
  - name: typos-lsp
    lspconfig_name: typos_lsp
    languages: [text, markdown]
    installer: [cargo, mason]
    provides_formatting: false
    install_command: "cargo install --locked --git https://github.com/tekumara/typos-lsp typos-lsp"

  # CI/CD
  - name: gh-actions-language-server
    lspconfig_name: gh_actions_ls
    languages: [yaml]
    installer: [npm, mason]
    provides_formatting: false

  # Containers
  - name: dockerfile-language-server
    lspconfig_name: dockerls
    languages: [dockerfile]
    installer: [mason]
    provides_formatting: false

  # AI copilot (opt-in via editors profile)
  - name: copilot-language-server
    lspconfig_name: copilot
    languages: [text]
    installer: [mason]
    provides_formatting: false
    profiles: [editors]

  # Generic diagnostic
  - name: diagnostic-languageserver
    lspconfig_name: diagnosticls
    languages: [text]
    installer: [npm, mason]
    provides_formatting: false

  # ============================================================
  # LANGUAGES PROFILE — LSPs for additional languages
  # ============================================================

  # Ruby
  - name: ruby-lsp
    lspconfig_name: ruby_lsp
    languages: [ruby]
    installer: [gem]
    provides_formatting: false
    profiles: [languages]

  # Zig
  - name: zls
    lspconfig_name: zls
    languages: [zig]
    installer: [brew, mason]
    provides_formatting: true
    profiles: [languages]

  # Prisma (ORM)
  - name: "@prisma/language-server"
    mason_name: prisma-language-server
    lspconfig_name: prismals
    languages: [prisma]
    installer: [npm, mason]
    provides_formatting: true
    profiles: [languages]

  # Vim
  - name: vim-language-server
    lspconfig_name: vimls
    languages: [vim]
    installer: [npm, mason]
    provides_formatting: false
    profiles: [languages]

  # ============================================================
  # EDITORS PROFILE — LSPs for alternative shells/editors
  # ============================================================

  # Fish shell
  - name: fish-lsp
    lspconfig_name: fish_lsp
    languages: [fish]
    installer: [mason]
    provides_formatting: false
    profiles: [editors]

  # ============================================================
  # EXTRAS PROFILE — Specialty LSPs
  # ============================================================

  # Web (HTML/CSS)
  - name: css-lsp
    lspconfig_name: cssls
    languages: [css, scss, sass]
    installer: [mason]
    provides_formatting: false
    profiles: [extras]

  - name: html-lsp
    lspconfig_name: html
    languages: [html]
    installer: [mason]
    provides_formatting: false
    profiles: [extras]

  # CMake
  - name: neocmakelsp
    lspconfig_name: neocmake
    languages: [cmake]
    installer: [mason]
    provides_formatting: true
    profiles: [extras]

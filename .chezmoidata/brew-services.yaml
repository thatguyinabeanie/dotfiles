# Brew Services Configuration
# Services that should always be running
# These will be started if stopped, restarted if running
#
# Service Configuration Options:
# - name: Service name (required)
# - description: Human-readable description (optional)
# - action: "start" (only start if stopped) or "restart" (always restart) (required)
# - enable: Controls whether the service should be managed (optional, defaults to true)
#   - true: Always manage the service (default if not specified)
#   - false: Stop the service if running, don't start it
#   - "ifInstalled": Only manage if the service binary is installed on the system

services:
  # macOS Enhancements (conditionally installed)
  - name: "sketchybar"
    description: "Custom macOS menu bar"
    action: "restart"
    enable: "ifInstalled"

  - name: "borders"
    description: "Window borders for macOS"
    action: "start"

  - name: "redis"
    enable: "false"
    description: "Redis in-memory data store"
    action: "start"

# Development Tools (uncomment if installed)
# - name: "postgresql@14"
#   description: "PostgreSQL database server"
#   action: "restart"

# - name: "mysql"
#   description: "MySQL database server"
#   action: "start"

# Window Management (uncomment if installed)
# - name: "yabai"
#   description: "Window management for macOS"
#   action: "restart"

# - name: "skhd"
#   description: "Hotkey daemon for macOS"
#   action: "restart"

# Networking & Sync (uncomment if installed)
# - name: "syncthing"
#   description: "File synchronization"
#   action: "start"

# - name: "tailscale"
#   description: "VPN service"
#   action: "start"

# Services to exclude from automatic management
# (they exist but should be managed manually)
excluded_services:
  - "docker"  # Often managed via Docker Desktop
  - "apache"  # Conflicts with nginx
  - "httpd"   # Apache alternative name

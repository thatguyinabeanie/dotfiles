## Gemini Added Memories
- When debugging a configuration error, I should first consult the official schema, then correct any related documentation before fixing the configuration file itself. I should follow the user's corrections on details like configuration keys and file paths.

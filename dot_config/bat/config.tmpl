# bat configuration
# See `bat --help` for more information

# Set the theme
--theme="Catppuccin {{ title .CATPPUCCIN_FLAVOR }}"

# Show line numbers
--style="numbers,changes,header"

# Use italic text (if terminal supports it)
--italic-text=always

# Enable paging (auto uses pager only when content exceeds screen)
--paging=auto

// ============================================================================
// Zellij Configuration (KDL Format)
// ============================================================================
// Terminal multiplexer with tmux-like keybindings (Ctrl+a prefix)
// Documentation: https://zellij.dev/documentation/
// ============================================================================

// ============================================================================
// THEME
// ============================================================================
theme "catppuccin-{{ .CATPPUCCIN_FLAVOR }}"

// ============================================================================
// DEFAULT SHELL
// ============================================================================
default_shell "{{ .env_shared.shell_paths.zsh }}"

// ============================================================================
// APPEARANCE
// ============================================================================

// Use simplified UI for a cleaner look
simplified_ui true

// Disable pane frames for minimal appearance
pane_frames false

// Use compact layout by default
default_layout "compact"

// Rounded corners for pane frames (when enabled)
ui {
    pane_frames {
        rounded_corners true
        hide_session_name true
    }
}

// ============================================================================
// BEHAVIOR
// ============================================================================

// Copy text to clipboard when selected
copy_on_select true

// Enable mouse support
mouse_mode true

// Scroll buffer size
scroll_buffer_size 10000

// What to do when Zellij receives a close signal
on_force_close "quit"

// Mirror session across all attached clients
mirror_session false

// ============================================================================
// SESSION
// ============================================================================

// Auto-attach to existing session
session_serialization true

// Where to store session data
// Uses XDG_DATA_HOME by default

// ============================================================================
// KEYBINDINGS (Ctrl+a prefix - tmux-like)
// ============================================================================

keybinds clear-defaults=true {
    // Normal mode - most actions happen here
    normal {
        // Ctrl+a enters "tmux" mode (prefix key)
        bind "Ctrl a" { SwitchToMode "Tmux"; }

        // Quick actions without prefix
        bind "Alt h" { MoveFocusOrTab "Left"; }
        bind "Alt l" { MoveFocusOrTab "Right"; }
        bind "Alt j" { MoveFocus "Down"; }
        bind "Alt k" { MoveFocus "Up"; }

        // Alt+n for new pane (quick access)
        bind "Alt n" { NewPane; }
    }

    // Tmux mode - activated with Ctrl+a
    tmux {
        // Double Ctrl+a sends literal Ctrl+a to the shell
        bind "Ctrl a" { Write 1; SwitchToMode "Normal"; }

        // Exit tmux mode
        bind "Esc" { SwitchToMode "Normal"; }
        bind "q" { SwitchToMode "Normal"; }

        // ========================================
        // TAB MANAGEMENT
        // ========================================
        // c - create new tab
        bind "c" { NewTab; SwitchToMode "Normal"; }

        // n/p - next/previous tab
        bind "n" { GoToNextTab; SwitchToMode "Normal"; }
        bind "p" { GoToPreviousTab; SwitchToMode "Normal"; }

        // Number keys - go to tab by index
        bind "1" { GoToTab 1; SwitchToMode "Normal"; }
        bind "2" { GoToTab 2; SwitchToMode "Normal"; }
        bind "3" { GoToTab 3; SwitchToMode "Normal"; }
        bind "4" { GoToTab 4; SwitchToMode "Normal"; }
        bind "5" { GoToTab 5; SwitchToMode "Normal"; }
        bind "6" { GoToTab 6; SwitchToMode "Normal"; }
        bind "7" { GoToTab 7; SwitchToMode "Normal"; }
        bind "8" { GoToTab 8; SwitchToMode "Normal"; }
        bind "9" { GoToTab 9; SwitchToMode "Normal"; }

        // , - rename tab
        bind "," { SwitchToMode "RenameTab"; TabNameInput 0; }

        // & - close tab
        bind "&" { CloseTab; SwitchToMode "Normal"; }

        // ========================================
        // PANE MANAGEMENT
        // ========================================
        // | or % - vertical split (left/right)
        bind "|" { NewPane "Right"; SwitchToMode "Normal"; }
        bind "%" { NewPane "Right"; SwitchToMode "Normal"; }

        // - or " - horizontal split (top/bottom)
        bind "-" { NewPane "Down"; SwitchToMode "Normal"; }
        bind "\"" { NewPane "Down"; SwitchToMode "Normal"; }

        // x - close pane
        bind "x" { CloseFocus; SwitchToMode "Normal"; }

        // z - toggle pane zoom (fullscreen)
        bind "z" { ToggleFocusFullscreen; SwitchToMode "Normal"; }

        // o - cycle through panes
        bind "o" { FocusNextPane; SwitchToMode "Normal"; }

        // Arrow keys - move focus
        bind "Left" { MoveFocus "Left"; SwitchToMode "Normal"; }
        bind "Right" { MoveFocus "Right"; SwitchToMode "Normal"; }
        bind "Up" { MoveFocus "Up"; SwitchToMode "Normal"; }
        bind "Down" { MoveFocus "Down"; SwitchToMode "Normal"; }

        // h/j/k/l - vim-style move focus
        bind "h" { MoveFocus "Left"; SwitchToMode "Normal"; }
        bind "j" { MoveFocus "Down"; SwitchToMode "Normal"; }
        bind "k" { MoveFocus "Up"; SwitchToMode "Normal"; }
        bind "l" { MoveFocus "Right"; SwitchToMode "Normal"; }

        // ========================================
        // RESIZE MODE
        // ========================================
        bind "r" { SwitchToMode "Resize"; }

        // ========================================
        // SESSION MANAGEMENT
        // ========================================
        // d - detach from session
        bind "d" { Detach; }

        // s - session manager
        bind "s" { SwitchToMode "Session"; }

        // ========================================
        // SCROLL / COPY MODE
        // ========================================
        // [ - enter scroll/copy mode
        bind "[" { SwitchToMode "Scroll"; }

        // ========================================
        // MISC
        // ========================================
        // : - command mode
        bind ":" { SwitchToMode "EnterSearch"; SearchInput 0; }

        // ? - help
        bind "?" { SwitchToMode "Normal"; }

        // Space - toggle floating pane
        bind "Space" { ToggleFloatingPanes; SwitchToMode "Normal"; }

        // f - toggle floating for current pane
        bind "f" { TogglePaneEmbedOrFloating; SwitchToMode "Normal"; }
    }

    // Resize mode
    resize {
        bind "Esc" { SwitchToMode "Normal"; }
        bind "q" { SwitchToMode "Normal"; }
        bind "Ctrl a" { SwitchToMode "Tmux"; }

        bind "h" "Left" { Resize "Increase Left"; }
        bind "j" "Down" { Resize "Increase Down"; }
        bind "k" "Up" { Resize "Increase Up"; }
        bind "l" "Right" { Resize "Increase Right"; }

        bind "H" { Resize "Decrease Left"; }
        bind "J" { Resize "Decrease Down"; }
        bind "K" { Resize "Decrease Up"; }
        bind "L" { Resize "Decrease Right"; }

        bind "=" { Resize "Increase"; }
        bind "-" { Resize "Decrease"; }
    }

    // Scroll mode (like tmux copy mode)
    scroll {
        bind "Esc" { SwitchToMode "Normal"; }
        bind "q" { SwitchToMode "Normal"; }
        bind "Ctrl a" { SwitchToMode "Tmux"; }

        bind "j" "Down" { ScrollDown; }
        bind "k" "Up" { ScrollUp; }
        bind "Ctrl d" { HalfPageScrollDown; }
        bind "Ctrl u" { HalfPageScrollUp; }
        bind "Ctrl f" { PageScrollDown; }
        bind "Ctrl b" { PageScrollUp; }

        // Enter search mode
        bind "/" { SwitchToMode "EnterSearch"; SearchInput 0; }

        // Copy mode
        bind "v" { SwitchToMode "EnterSearch"; }
    }

    // Search mode
    entersearch {
        bind "Esc" { SwitchToMode "Normal"; }
        bind "Ctrl c" { SwitchToMode "Scroll"; }
        bind "Enter" { SwitchToMode "Search"; }
    }

    search {
        bind "Esc" { SwitchToMode "Normal"; }
        bind "q" { SwitchToMode "Normal"; }
        bind "Ctrl a" { SwitchToMode "Tmux"; }

        bind "n" { Search "down"; }
        bind "N" { Search "up"; }
        bind "c" { SearchToggleOption "CaseSensitivity"; }
        bind "w" { SearchToggleOption "Wrap"; }
        bind "o" { SearchToggleOption "WholeWord"; }
    }

    // Session mode
    session {
        bind "Esc" { SwitchToMode "Normal"; }
        bind "q" { SwitchToMode "Normal"; }
        bind "Ctrl a" { SwitchToMode "Tmux"; }

        bind "d" { Detach; }
        bind "w" {
            LaunchOrFocusPlugin "session-manager" {
                floating true
                move_to_focused_tab true
            };
            SwitchToMode "Normal"
        }
    }

    // Rename tab mode
    renametab {
        bind "Esc" { UndoRenameTab; SwitchToMode "Normal"; }
        bind "Enter" { SwitchToMode "Normal"; }
    }

    // Shared bindings across all modes
    shared_except "normal" "tmux" {
        bind "Esc" { SwitchToMode "Normal"; }
    }
}

// ============================================================================
// PLUGINS
// ============================================================================

plugins {
    tab-bar location="zellij:tab-bar"
    status-bar location="zellij:status-bar"
    strider location="zellij:strider"
    compact-bar location="zellij:compact-bar"
    session-manager location="zellij:session-manager"
    welcome-screen location="zellij:session-manager" {
        welcome_screen true
    }
    filepicker location="zellij:strider" {
        cwd "/"
    }
}

{{- $data := .data -}}
{{- $schema := .schema -}}
{{- $global := .global -}}

{
{{- if $schema }}
  "$schema": "{{ $schema }}",
{{- end }}

{{- /* Top-level simple fields */ -}}
{{- if hasKey $data "model" }}
  "model": "{{ $data.model }}",
{{- end }}
{{- if hasKey $data "small_model" }}
  "small_model": "{{ $data.small_model }}",
{{- end }}
{{- if hasKey $data "theme" }}
  "theme": "{{ $data.theme }}",
{{- end }}
{{- if hasKey $data "share" }}
  "share": "{{ $data.share }}",
{{- end }}
{{- if hasKey $data "autoupdate" }}
  "autoupdate": {{ $data.autoupdate }},
{{- end }}

{{- /* Provider configurations - convert providers list to provider map */ -}}
{{- if hasKey $data "providers" }}
  "provider": {
{{- $providerMap := dict -}}
{{- range $data.providers -}}
  {{- $providerMap = set $providerMap . (dict "options" (dict)) -}}
{{- end -}}
{{- template "utilities/json-providers.tmpl" (dict "data" $providerMap "global" $global ) }}
  }
{{- end }}
}
